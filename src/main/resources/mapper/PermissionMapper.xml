<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.syan.smart_park.dao.PermissionMapper">

    <!-- 根据角色ID查询权限列表 -->
    <select id="selectPermissionsByRoleId" resultType="com.syan.smart_park.entity.Permission">
        SELECT p.*
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId}
          AND p.deleted = 0
          AND rp.deleted = 0
    </select>

    <!-- 根据用户ID查询权限列表 -->
    <select id="selectPermissionsByUserId" resultType="com.syan.smart_park.entity.Permission">
        SELECT DISTINCT p.*
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND p.deleted = 0
          AND rp.deleted = 0
          AND ur.deleted = 0
    </select>

    <!-- 根据权限编码查询权限 -->
    <select id="selectByPermissionCode" resultType="com.syan.smart_park.entity.Permission">
        SELECT *
        FROM sys_permission
        WHERE permission_code = #{permissionCode}
          AND deleted = 0
    </select>

</mapper>
